{
  "name": "renderly-api",
  "version": "1.0.0",
  "description": "Renderly — HTML to PDF API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "nanoid": "^3.3.7",
    "pg": "^8.12.0",
    "puppeteer": "^22.15.0",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0",
    "undici": "^6.19.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
