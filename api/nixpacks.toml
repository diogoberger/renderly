[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "freetype", "harfbuzz", "ca-certificates", "glib", "dbus"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "node src/db/migrate.js && node src/server.js"
